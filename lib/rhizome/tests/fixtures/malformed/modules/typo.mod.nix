# A top-level key nothing reads. `peers` is the pre-rename spelling of
# `plugins`, the case most likely to arrive from outside this repo — but
# the mount treats it as it would any other unrecognised key.

{
  peers = { };
}
