# Built-in `platforms.<block>` keys and the `_class` tag each one maps to.
# Plugins may extend this set (e.g. `classes.editor = "editor"`) but never
# remap an existing key.
{
  nixos = "nixos";
  darwin = "darwin";
  home-manager = "homeManager";
}
